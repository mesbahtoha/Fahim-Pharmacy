# Fahim Pharmacy — environment configuration
#
# CloudLinux Node.js Selector sets the PORT variable automatically.
# No secrets or API keys are required by this application.
#
# Copy to `.env` only if you need to override values locally.
PORT=3000